/**
 * UserData interface
 * 
 * This interface defines the structure of the user data object.
 * It includes the username and job title of the user.
 */

export interface UserData {
  /**
   * The username of the user.
   */
  username: string;

  /**
   * The job title of the user.
   */
  jobTitle: string;
}